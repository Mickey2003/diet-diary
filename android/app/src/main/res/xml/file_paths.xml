<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera capture temp files -->
    <external-cache-path name="camera_captures" path="." />
    <!-- Shared images from the JS bridge share() call -->
    <cache-path name="shared_images" path="shared_images/" />
    <!-- Downloads -->
    <external-files-path name="downloads" path="Downloads/" />
</paths>
